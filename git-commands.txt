git config --global user.name 'imad12345'
# the same username used for github

git config --global user.email 'bimh1997@gmail.com'
# the same email used for email

git remote add origin https://github.com/imad12345/git-testing.git
# it links the local repository to the remote repository in github servers


git remote -v
# check if the remote repo is good

